#define X_COOR -20
#define Y_COOR -50
#define Z_COOR 120
#define R_FLAG 33
#define FREE_SPACE 4
#define CONSTRUCT_TREE 1
#define BASIC_TAX 1120
#define BASIC_SCORE_TAX 400

#define TOTAL_GOLD 1200
#define TOTAL_SCORE 560

#define OWNER "yun"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1570545919"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "云屋");
set("long", @LONG
这是一座颇有南方园林特色的假山，堆砌假山用的石料都是上等的太湖石，从那么远运到这里，不知主人是真爱石呢还是真有钱！透过假山石的孔洞，可以看到绕过这座假山，东北方是一池清澈的湖水，波光粼粼，使得到了这里的人感到心境平和

LONG
);
        set("exits", ([
"out" : "/p/residence/cloud3.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
