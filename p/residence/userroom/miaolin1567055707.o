#define X_COOR -20
#define Y_COOR -30
#define Z_COOR 80
#define R_FLAG 7
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "miaolin"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1567055707"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "御风·妙法居");
set("long", @LONG
此处为妙琳真人的清秀之地，清洁简朴处处透露着一阵阵仙气，不为尘世之气所染，不失为修行的好地方
天地逍遥任我游
不为红尘饶清秀
但愿青灯心有明
不羡鸳鸯不羡仙

LONG
);
        set("exits", ([
"out" : "/p/residence/smile2.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
