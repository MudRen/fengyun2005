#define X_COOR -10
#define Y_COOR -40
#define Z_COOR 40
#define R_FLAG 17
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "xxxb"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1532362704"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "小宝屋");
set("long", @LONG
风风雨雨，风云城的中心广场依旧鲜亮如新。风道天街和云街地巷在这里交会，
四方立起了宏伟的风云天地四个祭坛，昔日的广场却并无多少变化，虽然中央的水
池已经干涸，虽然当年碧绿的池水和尾尾金鱼不复存在，留下的是个几丈方圆，深
不见底的大洞。水池旁的蟠龙摩天石柱饱经岁月沧桑，依然不屈不挠地耸立，石柱
上九条飞龙张牙舞爪，直欲冲天而去。往高处走一步，便是天下闻名的天机阁。

LONG
);
        set("exits", ([
"out" : "/p/residence/tower1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
