#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 80
#define R_FLAG 19
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX 140
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "diablo"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1575301772"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "水晶穹顶");
set("long", @LONG
金刚大门通体由无数闪耀晶面组成。其高耸千丈，中心镶嵌有一大块浑圆晶体，大门表面有极其美观的花纹镌刻，门顶有象征天使翅膀形状的双尖矗立体，大门两侧有坚固的柱子以及大量扶壁，而金刚大门两侧高耸的城墙则环绕保护着银光之城。
q
q

LONG
);
        set("exits", ([
"out" : "/p/residence/sword2.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
