#define X_COOR -20
#define Y_COOR -30
#define Z_COOR 240
#define R_FLAG 33
#define FREE_SPACE 4
#define CONSTRUCT_TREE 1
#define BASIC_TAX 1120
#define BASIC_SCORE_TAX 400

#define TOTAL_GOLD 1200
#define TOTAL_SCORE 560

#define OWNER "miao"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1570073638"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "球屋");
set("long", @LONG
巨型球屋是在一个环状的地基上建造而成的，房子由两个球屋构成，
各种需要能得到满足，除了主卧、客房以及车库，还有一间极大的厨房
和两个和卧室连接的浴室，同时还配备了一间游戏室。

LONG
);
        set("exits", ([
"out" : "/p/residence/smile6.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
