#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 360
#define R_FLAG 63
#define FREE_SPACE 2
#define CONSTRUCT_TREE 1
#define BASIC_TAX 280
#define BASIC_SCORE_TAX 200

#define TOTAL_GOLD 300
#define TOTAL_SCORE 240

#define OWNER "skysword"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1533474966"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "封刀赏梅处");
set("long", @LONG
小时候，殷剑平的愿望是是成为人人敬仰的盖世豪侠，惩恶扬善，笑傲武林。
但自从他遇到了命中注定的对头封寒月之后，这个念头就变成了妻子孩子热炕头，三餐小酒逗逗狗。一代豪侠就此变成了普通百姓，而他却自得其乐。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword9.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
