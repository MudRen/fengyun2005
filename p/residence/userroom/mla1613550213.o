#define X_COOR -20
#define Y_COOR -50
#define Z_COOR 40
#define R_FLAG 33
#define FREE_SPACE 2
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 200

#define TOTAL_GOLD 300
#define TOTAL_SCORE 240

#define OWNER "mla"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1613550213"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "黄金屋");
set("long", @LONG
进得楼来只见四周陈设皆为黑色四壁也是黑色除了南边有一扇窗微微透过些亮色外就只有屋中桌子上的油灯发出微弱的光靠窗边有一床榻上面铺着简单的被褥窗口传来一阵阵海涛声似在向你呼唤又似在倾诉什么你忍不住想侧耳聆听

LONG
);
        set("exits", ([
"out" : "/p/residence/cloud1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
