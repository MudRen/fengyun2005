#define X_COOR -20
#define Y_COOR -30
#define Z_COOR 40
#define R_FLAG 17
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "starsoul"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1508152608"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "小河畔");
set("long", @LONG

微风中传来泉水流动的声音，你不知不觉得走过去，在流水旁坐下来。流水
也许会干枯，却永远不会停下来，仿佛永远不知道厌倦，它那种活泼的生机

永恒不变。谈淡的烟雾从河水上升起，看来是那么温柔，那么美丽，如果你
纵身一跃跃入雾里，你的烦恼和痛苦岂非也很快地就会随着这烟雾消失？


LONG
);
        set("exits", ([
"out" : "/p/residence/smile1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
