#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 80
#define R_FLAG 33
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "oneblade"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1510059093"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "刀客联盟一号");
set("long", @LONG
在每个孤独的夜晚，都有一个没有知觉的轮回，慢慢化作没有记忆的涟漪。回荡在那没有彼岸的湖中，荆棘丛生的林间，有一朵不懈的花，是我心中永恒的红。点缀我单调的心田，蹉跎间的碎瓣，是哪个浪子。不经意的绊脚，将蹒跚的步履，收割得如此残缺。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword2.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
