#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 40
#define R_FLAG 33
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX 140
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "dummya"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1508219327"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "大米帮");
set("long", @LONG
一张苏州水磨的长桌挨了画儿，桌子上摆着许多的古董，又摆着《如意君传》、《娇红记》、《三妙传》，各样的春意图儿。梳头桌子上放着象牙镶嵌的豆柏楠减妆一个，上面铺了一张斑竹万字床，挂了项月白百蝶湖罗帐子，床上铺了一领绝细的席子，放了一个长藤枕，两眼花丝细的单被，把沉速香薰得喷鼻子香的枕头边放着一个宋朝金胎雕漆双头牡丹花小圆盒，盒儿里面盛着真正缅甸国来的缅铃一个

LONG
);
        set("exits", ([
"out" : "/p/residence/sword1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
