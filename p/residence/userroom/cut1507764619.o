#define X_COOR -20
#define Y_COOR -30
#define Z_COOR 40
#define R_FLAG 17
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "cut"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1507764619"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "萧十一郎的家");
set("long", @LONG

这间小木屋依山而建，便是大盗萧十一郎的家了。除了一张木床外，
屋子里几乎什么都没有，显得说不出的冷清，说不出的空虚，每次

萧十一郎回到这里来，开始时也许会觉得很宁静。但到了后来，他
的心反而更乱了。没有人的地方，岂不也是孤独的地方？  


LONG
);
        set("exits", ([
"out" : "/p/residence/smile1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
