#define X_COOR -20
#define Y_COOR -30
#define Z_COOR 40
#define R_FLAG 1
#define FREE_SPACE 3
#define CONSTRUCT_TREE 1
#define BASIC_TAX 560
#define BASIC_SCORE_TAX 300

#define TOTAL_GOLD 600
#define TOTAL_SCORE 380

#define OWNER "cmoo"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1513248140"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "无尘居");
set("long", @LONG
一尘不染的木地板上，放着松软的鹅绒垫。窗外云霞灿烂，一望无际的海天浑然一体，隐隐有海涛声送进来，让人只想好好的睡上一觉。

LONG
);
        set("exits", ([
"out" : "/p/residence/smile1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
