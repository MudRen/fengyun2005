#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 360
#define R_FLAG 63
#define FREE_SPACE 2
#define CONSTRUCT_TREE 1
#define BASIC_TAX 280
#define BASIC_SCORE_TAX 200

#define TOTAL_GOLD 300
#define TOTAL_SCORE 240

#define OWNER "gulun"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1535284202"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "磐沙堡");
set("long", @LONG
从罗喉回到人界，古伦德变得比以前更加成熟。什么国仇家恨对于他来说，已经看得很轻。
所以，他回到磐沙堡，拿起老师葛梅尔曾经阅读过的东西典籍，想要从书中找到更多的大义。
然而，皓月当空的时候，他常会在窗边抬首注目，没有人知道，他此刻是在思念谁。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword9.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
