#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 360
#define R_FLAG 63
#define FREE_SPACE 2
#define CONSTRUCT_TREE 1
#define BASIC_TAX 280
#define BASIC_SCORE_TAX 200

#define TOTAL_GOLD 300
#define TOTAL_SCORE 240

#define OWNER "purple"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1533312513"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "琴剑双绝谷");
set("long", @LONG
自古以来，仙凡两界，不能相通。此乃金科玉律，为所有仙人所恪守。然而，在此古语之后还有两句：若能相通，便可成圣。是以，自古以来由凡入仙者众，而从圣飞升者寡。紫枫以琴剑入道，新晋真仙，转而便重回人世，竟是要走仙凡相通的从圣之道。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword9.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
