#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 240
#define R_FLAG 32
#define FREE_SPACE 4
#define CONSTRUCT_TREE 1
#define BASIC_TAX 1120
#define BASIC_SCORE_TAX 400

#define TOTAL_GOLD 1200
#define TOTAL_SCORE 560

#define OWNER "rosyc"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1575358811"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "侠客居");
set("long", @LONG
风风雨雨，风云城的中心广场依旧鲜亮如新。
四方立起了宏伟的风云天地四个祭坛，昔日的广场却并无多少变化，
虽然中央的水池已经干涸，虽然当年碧绿的池水和尾尾金鱼不复存在，留下的是个几丈方圆。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword6.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
