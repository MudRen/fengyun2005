#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 360
#define R_FLAG 63
#define FREE_SPACE 2
#define CONSTRUCT_TREE 1
#define BASIC_TAX 280
#define BASIC_SCORE_TAX 200

#define TOTAL_GOLD 300
#define TOTAL_SCORE 240

#define OWNER "polaris"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1533632634"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "时轮小筑");
set("long", @LONG
来往的人们都惊讶于这件小竹屋的名字，它显然与西域鼎鼎大名的时轮宫有着深厚的关系。不过坐镇于此的并不是垂眉老僧，而是个二十岁左右的女尼。
再后来，有好事者八卦出，原来这个女尼竟然是消失了足足有二十多年的慕容璇玑。从罗喉回来之后，她便孤身一人隐居于此，不问世事，青灯伴佛。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword9.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
