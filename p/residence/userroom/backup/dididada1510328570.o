#define X_COOR -20
#define Y_COOR -30
#define Z_COOR 40
#define R_FLAG 49
#define FREE_SPACE 5
#define CONSTRUCT_TREE 1
#define BASIC_TAX 2240
#define BASIC_SCORE_TAX 500

#define TOTAL_GOLD 2400
#define TOTAL_SCORE 820

#define OWNER "dididada"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1510328570"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "雾失山庄");
set("long", @LONG
《玉女心经》须得二人同练，互为辅助。练功时全身热气蒸腾，须拣空旷无人之处，全身衣服畅开而修习，使得热气立时发散，无片刻阻滞，否则转而郁积体内，小则重病，大则丧身。
这门内功步步艰难，时时刻刻会练入岔道，若无旁人相助，非走火入魔不可，只有你助我、我助你，合二人之力方能共渡险关。

LONG
);
        set("exits", ([
"out" : "/p/residence/smile1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
