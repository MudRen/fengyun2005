#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 360
#define R_FLAG 63
#define FREE_SPACE 2
#define CONSTRUCT_TREE 1
#define BASIC_TAX 280
#define BASIC_SCORE_TAX 200

#define TOTAL_GOLD 300
#define TOTAL_SCORE 240

#define OWNER "luna"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1533400257"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "狐狸窝");
set("long", @LONG
作为水母天姬的关门弟子，封寒月有着不可思议的天赋，她能将神水宫的媚术发挥到极致，全天下没有任何人可以抵挡。就算是水母天姬本人，也在见识过她的媚术之后，忍不住亲了她的面颊。
在离开神水宫之后，封寒月在此开了个销魂窟，但一切并不是看上去那么简单。她的一举一动，似乎都是为了某个暂不为人知的秘密行动。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword9.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
