#define X_COOR -10
#define Y_COOR -40
#define Z_COOR 120
#define R_FLAG 33
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX 140
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "lbb"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1558971016"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "核心价值观");
set("long", @LONG
党的十八大以来中央高度重视培育和践行社会主义核心价值观习近平总书记多次作出重要论述提出明确要求中央政治局围绕培育和弘扬社会主义核心价值观弘扬中华传统美德进行集体学习

LONG
);
        set("exits", ([
"out" : "/p/residence/tower3.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
