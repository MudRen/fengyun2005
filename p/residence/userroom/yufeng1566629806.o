#define X_COOR -20
#define Y_COOR -50
#define Z_COOR 40
#define R_FLAG 238
#define FREE_SPACE 1
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 100

#define TOTAL_GOLD 150
#define TOTAL_SCORE 120

#define OWNER "yufeng"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1566629806"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "御风居");
set("long", @LONG
房间收拾得十分整洁墙角边放一张简单的床铺一头是棋盘格花纹的帐幔另一头却只有粉刷的墙壁地下铺着泥砖真是一尘不染再也不肯干了

LONG
);
        set("exits", ([
"out" : "/p/residence/cloud1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
