#define X_COOR -30
#define Y_COOR -40
#define Z_COOR 360
#define R_FLAG 63
#define FREE_SPACE 2
#define CONSTRUCT_TREE 1
#define BASIC_TAX 280
#define BASIC_SCORE_TAX 200

#define TOTAL_GOLD 300
#define TOTAL_SCORE 240

#define OWNER "farseer"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1533295301"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "上官将军府");
set("long", @LONG
告老还乡之后，上官大将军便隐居于此，朝中无人知晓，自是落得清净安生。他的儿子上官文随了名字，成为一名举人，官拜三品，去了京城。于是，这曾经的将军府就变得更加冷清起来。
现如今，这里迎来了新的主人，上官大将军的孙子，上官远。和父亲不同，他似乎更喜欢舞刀弄剑，于是这府邸又渐渐多了些刀剑的肃杀气氛。

LONG
);
        set("exits", ([
"out" : "/p/residence/sword9.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
