#define X_COOR -20
#define Y_COOR -50
#define Z_COOR 40
#define R_FLAG 3
#define FREE_SPACE 3
#define CONSTRUCT_TREE 1
#define BASIC_TAX -1
#define BASIC_SCORE_TAX 300

#define TOTAL_GOLD 600
#define TOTAL_SCORE 380

#define OWNER "zhuhuan"
#define TIMESTAMP "1"
#define TIMESTAMP2 "1504194380"
#define CLASS "annie"

#include <ansi.h>

inherit XROOM;

void create()
{
set("short", "听雨轩");
set("long", @LONG
空谷溪行，缓缓潺潺
雾一点 雨一点 梦一点
青青翠竹，轻轻醉，悠悠琴音，幽幽情
踏歌起舞，柔声细语
风一缕 香一缕 诗一缕
一抹氤氲，一曳生姿，多少回，渐次飘逸
步履轻盈，亦梦亦幻，君若相思无觅处，何妨竹林深处寻

LONG
);
        set("exits", ([
"out" : "/p/residence/cloud1.c",
]));
set("objects", ([
       ]) );
	set("coor/x",X_COOR);
	set("coor/y",Y_COOR);
	set("coor/z",Z_COOR);
	set("room_flag",R_FLAG);
	set("owner", OWNER);
	set("class", CLASS);
	set("timestamp",TIMESTAMP);
	set("timestamp2",TIMESTAMP2);
	set("free_space",FREE_SPACE);
	set("basic_tax",BASIC_TAX);
	set("basic_score_tax",BASIC_SCORE_TAX);
	set("tree",CONSTRUCT_TREE);
	set("total_gold",TOTAL_GOLD);
	set("total_score",TOTAL_SCORE);
	setup();

}
