// 本文件定义了 幻境一 树林的最大房间号。
// 房间号必须连续，并且以 1 开始，至本数字结束。
// 必须配有该文件，建议由下述指令生成文件。

// 需要调整 幻境一 的树林尺寸时，设置本文件，并在幻境入口处
// 使用指令 (create_room_file) 即可！！！
// 务必注意，该数字必须整十，没有纠错设置！


// 本文件必须放置于  npc_obj 目录内。
// 主目录调用时，使用如： #include "npc_obj/hj_settings/room_amount_hj.h" 语句。

#define        HJ_ROOM_AMOUNT        60

